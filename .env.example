# Server Configuration
PORT=2000

# Database Configuration (for Prisma)
# Ensure username, password, and database_name match your PostgreSQL credentials.
# The default PostgreSQL port is 5432, not 3333. Please double-check your port if it's different.
DATABASE_URL="postgresql://username:password@localhost:5432/database_name"

# JWT (JSON Web Token) Configuration
# Replace 'your_jwt_secret' with a strong, complex random string!
JWT_SECRET="your_jwt_secret"
# JWT expiration time (e.g., 1h for 1 hour, 7d for 7 days, 30m for 30 minutes)
JWT_EXPIRATION=1h